<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>devOps</groupId>
	<artifactId>devOps</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>devOps</name>
	<url>http://maven.apache.org</url>

	<properties>

		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<java.version>1.8</java.version>

		<!-- SONARQUBE SECTION -->
		<!--  <sonar.login>pasquale.labate</sonar.login>
		<sonar.password>Chiave2019!3</sonar.password> -->
		<sonar.login>58a193c08d6ee582ab6d8f3ef5cad7f6695fe9da</sonar.login>
		<sonar.password></sonar.password> 
		<sonar.projectKey>devOps_pipeline_test</sonar.projectKey>
		<sonar.host.url>http://sonar02:9990</sonar.host.url>
		<sonar.java.binaries>bin</sonar.java.binaries>
		
		<!-- JACOCO/SONAR SECTION -->
		<sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
		<sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
		<sonar.jacoco.reportPaths>${project.build.directory}/jacoco.exec</sonar.jacoco.reportPaths>
		<sonar.language>java</sonar.language>

		<!-- TODO DA CONTROLLARE <sonar.junit.reportsPath>${project.build.directory}/jacoco.exec</sonar.junit.reportsPath> -->
		<sonar.junit.reportPaths>${project.build.directory}/jacoco.exec</sonar.junit.reportPaths>
	</properties>
	
	<profiles>
		<profile>
			<id>test</id>
			<activation>
				<jdk>${java.version}</jdk>
			</activation>
			<properties>
				<env>test</env>
				<sonar.host.url>http://sonar02:9990</sonar.host.url>
				<!-- USATI PER LA FASE DI DEPLOY SU APPLICATION SERVER -->
				<!-- <appsrv.username>plabate</appsrv.username>
				<appsrv.psw>Chiave2019!3!</appsrv.psw>
				<appsrv.host>http://cisrv02:8080</appsrv.host>  -->
			</properties>
		</profile>
	</profiles>
	
	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.sonarsource.scanner.maven</groupId>
					<artifactId>sonar-maven-plugin</artifactId>
					<version>3.6.0.1398</version>
				</plugin>
				
				<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.2</version>
				<executions>
					<execution>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>prepare-package</phase>
						<goals>
							<goal>report</goal>
						</goals>

						<configuration>
							<!-- Sets the output directory for the code coverage report. -->
							<outputDirectory>${project.reporting.outputDirectory}/jacoco</outputDirectory>
						</configuration>

					</execution>
				</executions>
			</plugin>
				
			</plugins>
		</pluginManagement>
	</build>

	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.12</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

</project>
